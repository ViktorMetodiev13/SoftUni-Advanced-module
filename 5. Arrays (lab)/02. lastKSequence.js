function lastKSequence(length, collectible) {
    let resultArr = [1];

    while (resultArr.length <= length) {
        if (resultArr.length <= collectible) {
            for (let index = 0; index < collectible; index++) {
                
            }
        } else {

        }
    }
}
lastKSequence(6, 3);